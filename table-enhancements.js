// Table enhancements: sortable columns + sticky totals footer + project locations
(function(){
  let sortKey='mw', sortDir='desc', locationData={projects:[]}, locations=new Map();
  const cols=['end_customer','status','area','mw','industry','assumed_location','station','area_plan','grid_customer','statnett_case','date'];
  const labels=['Sluttkunde','Status','NO','MW','Næring','Antatt lokasjon','Stasjon','Områdeplan','Nettselskap','Statnett sak','Dato'];
  function rebuildLocations(){locations=new Map();for(const p of(locationData.projects||[])){const k=String(p.statnett_case||'').trim();if(k)locations.set(k,p)}}
  function locFor(p){return locations.get(String(p.statnett_case||'').trim())||null}
  function locText(p){const l=locFor(p);return l?(l.location_name||l.address||'Ikke sikkert identifisert'):(String(p.industry||'').toLowerCase()==='datasenter'?'Ikke sikkert identifisert':'–')}
  function locBadge(p){const l=locFor(p);if(!l)return String(p.industry||'').toLowerCase()==='datasenter'?'<span class="loc-main unresolved">Ikke sikkert identifisert</span>':'–';const cls=l.confidence==='confirmed'?'confirmed':l.confidence==='area'?'area':l.confidence==='unresolved'?'unresolved':'probable';return '<span class="loc-main '+cls+'">'+escapeHtml(l.location_name||l.address||'Ikke sikkert identifisert')+'</span><span class="loc-confidence">'+escapeHtml(l.confidence_label||'')+'</span>'}
  function val(p,k){if(k==='mw')return Number(p.mw)||0;if(k==='date'){const m=String(p.date||'').match(/^(\d{2})\.(\d{2})\.(\d{4})$/);return m?new Date(+m[3],+m[2]-1,+m[1]).getTime():0;}if(k==='assumed_location')return locText(p).toLocaleLowerCase('nb');return String(p[k]??'').toLocaleLowerCase('nb');}
  function sorted(ps){return [...ps].sort((a,b)=>{const av=val(a,sortKey),bv=val(b,sortKey);let c=typeof av==='number'?av-bv:av.localeCompare(bv,'nb',{numeric:true});return sortDir==='asc'?c:-c})}
  function installHead(){const tr=document.querySelector('#projects thead tr');if(!tr)return;tr.innerHTML=labels.map((l,i)=>'<th data-sort="'+cols[i]+'" style="cursor:pointer;user-select:none" title="Klikk for å sortere">'+l+' <span class="sortmark"></span></th>').join('');tr.querySelectorAll('th').forEach(th=>th.addEventListener('click',()=>{const k=th.dataset.sort;sortDir=(sortKey===k&&sortDir==='desc')?'asc':'desc';sortKey=k;render()}));updateMarks();}
  function updateMarks(){document.querySelectorAll('#projects th[data-sort]').forEach(th=>{const s=th.querySelector('.sortmark');if(s)s.textContent=th.dataset.sort===sortKey?(sortDir==='asc'?'▲':'▼'):''})}
  window.renderTable=function(ps){updateMarks();const rows=sorted(ps),total=rows.reduce((s,p)=>s+(Number(p.mw)||0),0);els.countHint.textContent=rows.length+' saker i utvalget · '+fmt(total)+' totalt';els.tbody.innerHTML=rows.map(p=>'<tr><td><b>'+escapeHtml(p.end_customer||p.grid_customer||'–')+'</b></td><td><span class="status s-'+escapeHtml(p.status)+'">'+escapeHtml(p.status)+'</span></td><td>'+escapeHtml(p.area||'–')+'</td><td><b>'+escapeHtml(p.mw??'–')+'</b></td><td>'+escapeHtml(p.industry||'–')+'</td><td class="location-cell">'+locBadge(p)+'</td><td>'+escapeHtml(p.station||'–')+'</td><td>'+escapeHtml(p.area_plan||'–')+'</td><td>'+escapeHtml(p.grid_customer||'–')+'</td><td>'+escapeHtml(p.statnett_case||'–')+'</td><td>'+escapeHtml(p.date||'–')+'</td></tr>').join('')+'<tr class="total-row"><td><b>SUM</b></td><td><b>'+rows.length+' saker</b></td><td></td><td><b>'+Math.round(total).toLocaleString('nb-NO')+'</b></td><td colspan="7"></td></tr>';};
  async function loadLocations(){try{const r=await fetch('./data/datacenter-locations.json?v='+Date.now(),{cache:'no-store'});if(r.ok){locationData=await r.json();rebuildLocations();if(typeof render==='function')render();}}catch(e){console.warn('table locations',e)}}
  function defaultProjectLayerOff(){let tries=0;const timer=setInterval(()=>{const cb=document.getElementById('toggleProjects');if(cb){clearInterval(timer);if(cb.checked){cb.checked=false;cb.dispatchEvent(new Event('change',{bubbles:true}));}}else if(++tries>40)clearInterval(timer);},100);}
  const style=document.createElement('style');style.textContent='.sortmark{font-size:9px;margin-left:4px;color:#0a7052}.total-row td{position:sticky;bottom:0;background:#edf5f1!important;border-top:2px solid #bfd3c9;font-weight:800;z-index:3}.total-row td:first-child{color:#0a7052}.location-cell{white-space:normal;min-width:190px;max-width:270px}.loc-main{display:block;font-weight:700;line-height:1.25}.loc-main.confirmed{color:#5b3ca6}.loc-main.probable{color:#7055aa}.loc-main.area{color:#805d0c}.loc-main.unresolved{color:#7b8580;font-weight:600}.loc-confidence{display:block;margin-top:2px;color:#7b8580;font-size:9px;font-weight:700}';document.head.appendChild(style);
  installHead();loadLocations();defaultProjectLayerOff();if(typeof render==='function')render();
})();
